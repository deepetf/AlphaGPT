# AlphaGPT V2.1 Dependencies

# Deep Learning
torch>=2.0.0

# Data Processing
pandas>=2.0.0
numpy>=1.20.0
pyarrow>=12.0.0  # Required for reading Parquet files

# Utils
tqdm
python-dotenv  # If used for ENV loading
loguru         # Optional, kept for compatibility
